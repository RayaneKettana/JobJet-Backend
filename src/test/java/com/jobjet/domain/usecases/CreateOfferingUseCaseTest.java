package com.jobjet.domain.usecases;

import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

class CreateOfferingUseCaseTest {

    private CreateOfferingUseCase createOfferingUseCase;

    @BeforeEach
    void setUp() {
//        createOfferingUseCase = new CreateOfferingUseCase();
    }

    @Test
    @DisplayName("shouldAssertSomething")
    public void shouldAssertSomething() {
//        createOfferingUseCase.execute()
        Assertions.assertThat(false).isTrue();
    }

}